import { Stack, styled } from "@mui/material";

const CalendarControlStack = styled(
  Stack,
  {},
)({
  height: 40,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
})

export default CalendarControlStack;